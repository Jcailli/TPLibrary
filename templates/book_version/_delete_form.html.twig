<form method="post" action="{{ path('app_book_version_delete', {'id': book_version.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_version.id) }}">
    <button class="btn btn-danger icon-link"><twig:ux:icon class="bi" name="bi:trash" /> Delete</button>
</form>
