{% extends 'base.html.twig' %}

{% block title %}BookVersion{% endblock %}

{% block body %}
    <h1>BookVersion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_version.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book_version.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_book_version_index') }}">back to list</a>

    <a href="{{ path('app_book_version_edit', {'id': book_version.id}) }}">edit</a>

    {{ include('book_version/_delete_form.html.twig') }}
{% endblock %}
